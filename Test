// // ค่าเริ่มต้นของ dropdown
// const initialDropdownText = 'ทั้งหมด';

// // dropdown และ ค้นหา
// function filterCardItems(category, searchTerm) {
//     filteredCards = allCards.filter(card => {
//         const title = card.Desc.trim().toLowerCase();
//         const categoryMatch = category === initialDropdownText || title.includes(category.toLowerCase());
//         const searchTermMatch = searchTerm === '' || title === searchTerm.toLowerCase(); // เปลี่ยนเงื่อนไขนี้

//         return categoryMatch && searchTermMatch;
//     });

//     totalItems = filteredCards.length;
//     totalPages = Math.ceil(totalItems / itemsPerPage);

//     if (filteredCards.length > 0) {
//         changePage(1);
//     } else {
//         displayNoResultsMessage();
//     }
// }

// // ฟังก์ชันสำหรับการค้นหา
// function searchCardItems(category, searchTerm) {
//     filteredCards = allCards.filter(card => {
//         const title = card.Desc.trim().toLowerCase();
//         const categoryMatch = category === initialDropdownText || title.includes(category.toLowerCase());
//         const searchTermMatch = searchTerm === '' || title === searchTerm.toLowerCase(); // เปลี่ยนเงื่อนไขนี้

//         return categoryMatch && searchTermMatch;
//     });

//     totalItems = filteredCards.length;
//     totalPages = Math.ceil(totalItems / itemsPerPage);

//     if (filteredCards.length > 0) {
//         changePage(1);
//     } else {
//         displayNoResultsMessage();
//     }
// }

// // ฟังก์ชันสำหรับแสดงข้อความ "ไม่พบข้อมูล"
// function displayNoResultsMessage() {
//     const container = document.getElementById('card-container');
//     container.innerHTML = '<p class="text-center">ไม่พบข้อมูล</p>';
//     const paginationContainer = document.getElementById('pagination-container');
//     paginationContainer.innerHTML = '';
// }